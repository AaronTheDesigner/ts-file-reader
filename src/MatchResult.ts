// enum - enumeration = an object specifically meant to store closely realated values
// YAY ENUM !!!
export enum MatchResult  { // <== signals to other engineers that this is a collection of closely realated values
    HomeWin = 'H', // <== this and other enums are now a type within the application
    AwayWin = 'A',
    Draw = 'D'
};